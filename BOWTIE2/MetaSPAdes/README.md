En primer lugar, hay que indexar la referencia (IndexRef.sh).

Después, mapeamos la reads (obtenidas tras analizar la calidad y hacer el trimming) a esa referencia indexada (Bowtie2_MetaSPAdes.sh). 

Para ejecutar el script Bowtie2.sh en paralelo con todas las muestras usamos el script oneline_Bowtie2_MetaSPAdes.sh.
